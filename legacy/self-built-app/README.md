# Legacy Self-Built App

This directory keeps the deprecated WeCom self-built app HTTP API experiments.

Current mainline:

- Local chat reading: `decrypt/`
- Active operations: WecomTeam `wecom-cli/wecom-unified`
- Real-time interaction: future WecomTeam Bot SDK integration

Use these scripts only when intentionally restoring the old CorpID/Secret/AgentId flow.
