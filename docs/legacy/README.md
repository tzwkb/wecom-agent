# Legacy 自建应用方案

本目录保留旧的企业微信自建应用 HTTP API 方案，仅作历史参考。

当前主线已改为：

- 本地历史聊天读取：本仓库 `decrypt/`
- 主动操作：WecomTeam `wecom-cli/wecom-unified`
- 实时互动：后续接 WecomTeam Bot SDK

不要为普通发消息、通讯录、文档、表格、日程、会议、待办任务要求配置 CorpID/Secret/AgentId、可信域名或可信 IP。
